name: CD

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Nightly deploy
        run: curl -X POST -d {} https://api.netlify.com/build_hooks/5eea5a852a0c9301f8cf6009
  
